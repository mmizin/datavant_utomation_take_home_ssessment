export const BaseLocators = {
    navigationMenu: {
        buyTicketsTab: 'a:has(img[alt="Buy Tickets"])'
    },

    calendarPopup: {
        datePickerWidget: '.picker__holder:visible',
        closeButton: '.picker__button--close:not([disabled])'
    }
}